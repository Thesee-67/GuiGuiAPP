# ============================================
# GuiGuiTraining - Configuration DEV LOCAL
# ============================================
# Utiliser SQLite, emails désactivés, mode debug activé
# ============================================

# === APPLICATION ===
APP_NAME="GuiGuiTraining"
APP_VERSION="1.0.0"
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=True
ENVIRONMENT=development
APP_URL=http://localhost:8000

# === SÉCURITÉ ===
SECRET_KEY=[*jo?D06>2+T73Cmrd!<{g.}}b60fYh>
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_HOURS=24
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# === BASE DE DONNÉES (SQLite pour dev local) ===
DATABASE_TYPE=sqlite
DB_HOST=localhost
DB_PORT=3306
DB_NAME=training_escalade
DB_USER=training_user
DB_PASSWORD=password

# === EMAIL SERVICE (désactivé pour dev local) ===
SMTP_ENABLED=False
SMTP_HOST=localhost
SMTP_PORT=465
SMTP_USE_TLS=True
SMTP_USERNAME=test@localhost
SMTP_PASSWORD=password
SMTP_FROM_EMAIL=test@localhost
SMTP_FROM_NAME=GuiGuiTraining

# === VÉRIFICATION EMAIL (désactivée pour dev local) ===
EMAIL_VERIFICATION_REQUIRED=False
EMAIL_VERIFICATION_TOKEN_EXPIRE_HOURS=24

# === RESET PASSWORD ===
PASSWORD_RESET_TOKEN_EXPIRE_HOURS=2

# === UPLOADS ===
UPLOAD_DIR=public_html/uploads
MAX_UPLOAD_SIZE_MB=10
ALLOWED_IMAGE_EXTENSIONS=jpg,jpeg,png,webp

# === RATE LIMITING (désactivé pour dev local) ===
RATE_LIMIT_ENABLED=False
RATE_LIMIT_PER_MINUTE=60

# === BACKUP ===
BACKUP_ENABLED=True
BACKUP_RETENTION_DAYS=30

# === LOGS ===
LOG_LEVEL=DEBUG
LOG_FILE=logs/app.log

# === PREMIER ADMIN ===
FIRST_ADMIN_EMAIL=olivier_guittet@guigui.climbingthenet.fr
FIRST_ADMIN_USERNAME=olivier
FIRST_ADMIN_PASSWORD=MotDePasseTemporaire2024!